# Direct requirements for text_embedding service.
# Pinned to the tested versions running on this machine.
#
# The launcher installs the embedding backend package separately:
# - CPU/default path: fastembed from requirements-fastembed.txt
# - GPU path on NVIDIA hosts: fastembed-gpu at runtime
#
# To update after upgrading packages:
#   .venv/bin/pip freeze | grep -v '^-e' > requirements-full-lock.txt
#   (then extract direct deps back into this file)
fastapi==0.135.2
uvicorn==0.42.0
