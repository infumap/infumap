# Direct requirements for pdf_extract service.
# Pinned to the reviewed versions so first install does not drift to whatever
# happens to be latest on PyPI that day.
#
# pypdfium2 is imported by app.py for password-protected PDF detection.
# pdftext 0.7.1 (pulled by marker-pdf 2.0.0) requires exactly 5.10.1.
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.32
pypdfium2==5.10.1
marker-pdf[full]==2.0.0
